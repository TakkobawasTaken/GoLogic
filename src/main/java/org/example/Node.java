package org.example;

import java.util.ArrayList;

public class Node {
   /*
    int[][] board;
    int player;
    int visits;
    double score;
    List<GoNode> children;
    GoNode parent;


    public GoNode(int[][] board, int player, GoNode parent) {
        this.board = board;
        this.player = player;
        this.visits = 0;
        this.score = 0;
        this.children = new ArrayList<>();
        this.parent = parent;
    }

    public boolean isTerminal() {
        // Check if the game is over
        // Return true if it is, false otherwise
    }

    public List<GoNode> getUntriedMoves() {
        // Get a list of legal moves that haven't been tried yet
        // Return an empty list if there are no untried moves
    }

    public GoNode selectChild() {
        // Use the UCB1 formula to select a child node to explore
        // Return the selected child node
    }

    public void expand() {
        // Expand the current node by adding a new child node for each untried move
        // Choose one of the untried moves randomly and add a new child node for it
    }

    public double simulate() {
        // Simulate a game from the current node to the end using random moves
        // Return the score of the game
    }

    public void update(double score) {
        // Update the node's statistics with the result of the simulation
        // Increase the visits count by 1
        // Update the total score by adding the simulation result
    }

    public GoNode getBestChild() {
        // Get the child node with the highest UCB1 value
        // Return null if there are no children
    }

    */
}
